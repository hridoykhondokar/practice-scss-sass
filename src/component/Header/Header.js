import React from 'react';
import './Header.scss';

const Header = () => {
    return (
        <div className='header-container'>
            <button >SASS PRACTICE</button>
            <p className='text'>this is header container</p> 
              
        </div>

    );
};

export default Header;